const config = {
    //数据库配置
    database: {
        DATABASE: 'phoneBook',
        USERNAME: 'root',
        PASSWORD: 'root',
        PORT: '3306',
        HOST: 'localhost'
    }
}
module. exports = config;